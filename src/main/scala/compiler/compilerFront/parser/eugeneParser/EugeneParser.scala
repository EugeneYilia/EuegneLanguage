package compiler.compilerFront.parser.eugeneParser

import compiler.compilerFront.common.Token

object EugeneParser {

  def parseToken(tokens: List[Token]): Unit = {

  }

  def apply(tokens: List[Token]): Unit = parseToken(tokens)
}
